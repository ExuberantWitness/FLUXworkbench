# Flux Studio brain — deps baked into the EMBEDDED python at build time
# (scripts/fetch-python.mjs). Users install nothing: one-click setup.
httpx>=0.27
cmsis-svd>=0.4          # ingest_svd (register maps)
pdfplumber>=0.11        # ingest_datasheet (reference-manual PDFs)
numpy>=1.26             # fluxweave_core (design_urdf geometry) — imported at MCP load

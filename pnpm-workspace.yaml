# Flux Workbench v2 — pnpm workspace (TS side: studio + headless + tailscale)
# Python side: see pyproject.toml. C side: see CMakeLists.txt.
packages:
  - "app"
  - "mod-headless"
  - "mod-tailscale"

linkWorkspacePackages: true
preferWorkspacePackages: true
